import React from 'react';
import { getCourses } from "../api/courseApi";

/**
 * @typedef {function(): JSX.Element} CoursesPage
 */
export function CoursesPage(props) {
    componentDidMount() {

        
    }
    return (
        <h2>Courses</h2>
    );
}